# ResourceFormatLoader
# ====================

# Godot's ResourceFormatLoader (inherits RefCounted), generated by tools/gen_api.py from
# extension_api.json; edit the generator, not this file. 0
# methods; left out: 11 virtual.
# An inherited method is in RefCounted.bend: objects are one type,
# so it takes a ResourceFormatLoader as it is.
import Base
import ../Godot.bend as Godot

def new() -> IO(Godot.Object):
  Godot.new("ResourceFormatLoader")

def CACHE_MODE_IGNORE() -> U32:
  0

def CACHE_MODE_REUSE() -> U32:
  1

def CACHE_MODE_REPLACE() -> U32:
  2

def CACHE_MODE_IGNORE_DEEP() -> U32:
  3

def CACHE_MODE_REPLACE_DEEP() -> U32:
  4
