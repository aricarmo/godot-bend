# ResourceImporter
# ================

# Godot's ResourceImporter (inherits RefCounted), generated by tools/gen_api.py from
# extension_api.json; edit the generator, not this file. 0
# methods; left out: 1 virtual.
# An inherited method is in RefCounted.bend: objects are one type,
# so it takes a ResourceImporter as it is.
import Base
import ../Godot.bend as Godot

def IMPORT_ORDER_DEFAULT() -> U32:
  0

def IMPORT_ORDER_SCENE() -> U32:
  100
