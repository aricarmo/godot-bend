# OpenXRSpatialComponentPlaneSemanticLabelList
# ============================================

# Godot's OpenXRSpatialComponentPlaneSemanticLabelList (inherits OpenXRSpatialComponentData), generated by tools/gen_api.py from
# extension_api.json; edit the generator, not this file. 1
# methods; left out: none.
# An inherited method is in OpenXRSpatialComponentData.bend: objects are one type,
# so it takes a OpenXRSpatialComponentPlaneSemanticLabelList as it is.
import Base
import ../Godot.bend as Godot

def new() -> IO(Godot.Object):
  Godot.new("OpenXRSpatialComponentPlaneSemanticLabelList")

def PLANE_SEMANTIC_LABEL_UNCATEGORIZED() -> U32:
  1

def PLANE_SEMANTIC_LABEL_FLOOR() -> U32:
  2

def PLANE_SEMANTIC_LABEL_WALL() -> U32:
  3

def PLANE_SEMANTIC_LABEL_CEILING() -> U32:
  4

def PLANE_SEMANTIC_LABEL_TABLE() -> U32:
  5

def get_plane_semantic_label(self: Godot.Object, index: U32) -> IO(U32):
  Godot.call_int(self, "get_plane_semantic_label", [Godot.VInt{index}])
