# ENetPacketPeer
# ==============

# Godot's ENetPacketPeer (inherits PacketPeer), generated by tools/gen_api.py from
# extension_api.json; edit the generator, not this file. 15
# methods; left out: 1 with an argument not carried yet.
# An inherited method is in PacketPeer.bend: objects are one type,
# so it takes a ENetPacketPeer as it is.
import Base
import ../Godot.bend as Godot

def STATE_DISCONNECTED() -> U32:
  0

def STATE_CONNECTING() -> U32:
  1

def STATE_ACKNOWLEDGING_CONNECT() -> U32:
  2

def STATE_CONNECTION_PENDING() -> U32:
  3

def STATE_CONNECTION_SUCCEEDED() -> U32:
  4

def STATE_CONNECTED() -> U32:
  5

def STATE_DISCONNECT_LATER() -> U32:
  6

def STATE_DISCONNECTING() -> U32:
  7

def STATE_ACKNOWLEDGING_DISCONNECT() -> U32:
  8

def STATE_ZOMBIE() -> U32:
  9

def PEER_PACKET_LOSS() -> U32:
  0

def PEER_PACKET_LOSS_VARIANCE() -> U32:
  1

def PEER_PACKET_LOSS_EPOCH() -> U32:
  2

def PEER_ROUND_TRIP_TIME() -> U32:
  3

def PEER_ROUND_TRIP_TIME_VARIANCE() -> U32:
  4

def PEER_LAST_ROUND_TRIP_TIME() -> U32:
  5

def PEER_LAST_ROUND_TRIP_TIME_VARIANCE() -> U32:
  6

def PEER_PACKET_THROTTLE() -> U32:
  7

def PEER_PACKET_THROTTLE_LIMIT() -> U32:
  8

def PEER_PACKET_THROTTLE_COUNTER() -> U32:
  9

def PEER_PACKET_THROTTLE_EPOCH() -> U32:
  10

def PEER_PACKET_THROTTLE_ACCELERATION() -> U32:
  11

def PEER_PACKET_THROTTLE_DECELERATION() -> U32:
  12

def PEER_PACKET_THROTTLE_INTERVAL() -> U32:
  13

def PACKET_LOSS_SCALE() -> U32:
  65536

def PACKET_THROTTLE_SCALE() -> U32:
  32

def FLAG_RELIABLE() -> U32:
  1

def FLAG_UNSEQUENCED() -> U32:
  2

def FLAG_UNRELIABLE_FRAGMENT() -> U32:
  8

def peer_disconnect(self: Godot.Object) -> IO(Unit):
  Godot.run(self, "peer_disconnect", [])

def peer_disconnect.all(self: Godot.Object, data: U32) -> IO(Unit):
  Godot.run(self, "peer_disconnect", [Godot.VInt{data}])

def peer_disconnect_later(self: Godot.Object) -> IO(Unit):
  Godot.run(self, "peer_disconnect_later", [])

def peer_disconnect_later.all(self: Godot.Object, data: U32) -> IO(Unit):
  Godot.run(self, "peer_disconnect_later", [Godot.VInt{data}])

def peer_disconnect_now(self: Godot.Object) -> IO(Unit):
  Godot.run(self, "peer_disconnect_now", [])

def peer_disconnect_now.all(self: Godot.Object, data: U32) -> IO(Unit):
  Godot.run(self, "peer_disconnect_now", [Godot.VInt{data}])

def ping(self: Godot.Object) -> IO(Unit):
  Godot.run(self, "ping", [])

def ping_interval(self: Godot.Object, ping_interval: U32) -> IO(Unit):
  Godot.run(self, "ping_interval", [Godot.VInt{ping_interval}])

def reset(self: Godot.Object) -> IO(Unit):
  Godot.run(self, "reset", [])

def throttle_configure(
  self: Godot.Object, interval: U32, acceleration: U32, deceleration: U32
) -> IO(Unit):
  Godot.run(self, "throttle_configure", [
    Godot.VInt{interval}, Godot.VInt{acceleration}, Godot.VInt{deceleration}])

def set_timeout(
  self: Godot.Object, timeout: U32, timeout_min: U32, timeout_max: U32
) -> IO(Unit):
  Godot.run(self, "set_timeout", [
    Godot.VInt{timeout}, Godot.VInt{timeout_min}, Godot.VInt{timeout_max}])

def get_packet_flags(self: Godot.Object) -> IO(U32):
  Godot.call_int(self, "get_packet_flags", [])

def get_remote_address(self: Godot.Object) -> IO(String):
  Godot.call_str(self, "get_remote_address", [])

def get_remote_port(self: Godot.Object) -> IO(U32):
  Godot.call_int(self, "get_remote_port", [])

def get_statistic(self: Godot.Object, statistic: U32) -> IO(F32):
  Godot.call_float(self, "get_statistic", [Godot.VInt{statistic}])

def get_state(self: Godot.Object) -> IO(U32):
  Godot.call_int(self, "get_state", [])

def get_channels(self: Godot.Object) -> IO(U32):
  Godot.call_int(self, "get_channels", [])

def is_active(self: Godot.Object) -> IO(Bool):
  Godot.call_bool(self, "is_active", [])
